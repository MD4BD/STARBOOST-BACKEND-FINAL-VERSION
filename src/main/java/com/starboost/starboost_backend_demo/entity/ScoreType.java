package com.starboost.starboost_backend_demo.entity;

public enum ScoreType {
    CONTRACT,   // points per contractType match
    PACK,       // points per packType match
    REVENUE     // points per revenueUnit
}